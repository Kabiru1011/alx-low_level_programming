
#include <stdio.h>
#include <main.h>
/**
 *print_square - print squres
 *@size: parameter
 *return: returns nothing
 */

void print_square(int size)
{
	int i,j;
	if (size <= 0>)
			_putchar(10);
			for(i = 0, i < size; i+)
			{
			for (j = 0, j < size; j++)
			{
				putcha('#')
			}
			putchar(\n)
			}
}
